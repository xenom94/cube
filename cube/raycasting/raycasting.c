//to convert a degree to rediant 

double degree_to_rad(double degree)
{
    double PI = 3.14;
    return (degree * (PI / 180.0));
 
}

double rad_to_degree(double radiant)
{
    double PI = 3.14;
    return (radiant * (180.0 / PI));

}

// the slice of the angle of each column (rays)

// rayAngle += fov_angle / num_rays

//function setup ; intialise all objects

// function update : update all game objects before we render the next frame 

//function draw -> update() : render all objects frame by  frame